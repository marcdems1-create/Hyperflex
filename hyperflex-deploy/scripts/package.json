{
  "name": "hyperflex-oracle-monitor",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "monitor": "node priceMonitor.js",
    "monitor:pm2": "pm2 start priceMonitor.js --name hyperflex-oracle && pm2 save"
  },
  "dependencies": {
    "ethers": "^6.0.0",
    "node-fetch": "^3.0.0",
    "dotenv": "^16.0.0"
  }
}
